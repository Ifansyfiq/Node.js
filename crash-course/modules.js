const {people,ages} = require('./people')

console.log(people, ages)// return empty object

const os = require('os') // built-in modules in js (one of them is os)
// console.log(os) //print os spec for the system

console.log(os.platform(), os.homedir()) 